# DEI: краткое состояние проекта

Обновлено: 2026-07-14.

## Сейчас

- Production: `https://dei-coral.vercel.app`.
- Последний локальный этап: каталог DEI упрощен до двух тестовых товаров.
- Админка товаров: `/admin/products`.
- Админка заказов: `/admin/orders`.
- База Neon подключена на production.
- Этап `2A` завершен: добавлены optional-поля товара и безопасная нормализация.
- Проверки после `2A`: `npm run lint` прошел с 6 старыми предупреждениями про изображения, `npm run build` прошел.
- Этап `2B` завершен: `/admin/products` умеет заполнять основные новые поля товара.
- Проверки после `2B`: `npm run lint` прошел с 6 старыми предупреждениями про изображения, `npm run build` прошел.
- Визуальный этап `V1` завершен: главная стала ближе к интернет-магазину, hero показывает реальный товар, блок популярных товаров ведет в каталог/заказ, карточки товаров стали плотнее.
- Визуальный этап `V2` завершен: добавлены премиальные свечения, градиенты, анимированные подсветки, усилен визуал каталога и страницы товара.
- Проверки после `V2`: `npm run lint` прошел с 6 старыми предупреждениями про изображения, `npm run build` прошел, локально `/`, `/catalog` и карточка товара отвечают `200`.
- Каталог `V3` завершен: отдельная витрина на главной удалена, в каталоге оставлены только два тестовых товара — `Сварочный аппарат Протон` и `Светодиодный светильник Кобра`; старые тестовые товары скрываются из файлового журнала и Neon-строк по slug.
- Проверки после `V3`: `npm run lint` прошел с 6 старыми предупреждениями про изображения, `npm run build` прошел, локально `/`, `/catalog` и обе новые карточки отвечают `200`.

## Рабочая цель

Развивать DEI как крупный интернет-магазин. Lead координирует агентов, принимает результат, проверяет и пушит. Рабочие агенты пишут только финал, блокер или важный риск. Пользователь получает короткую простую сводку только после готового этапа или перед риском лимита.

## Что уже работает

- Каталог, карточка товара, корзина, checkout.
- Заказы создаются и пересчитываются на сервере.
- Управляемый каталог товаров через админку.
- Production-деплой через Vercel.

## Главная слабость DEI

- Каталог пока тестовый: 2 товара с реальными фото от владельца.
- Не хватает SKU, наличия, сроков поставки, документов, нормальных характеристик, вариантов товара и SEO-полей.

## Следующий порядок работ

1. Визуально проверить новый упрощенный каталог на desktop/mobile и поправить спорные места.
2. Добавить реальные данные по двум товарам: цена, наличие, сроки, документы, характеристики, варианты.
3. Потом расширять каталог и оплату ЮKassa.

## Экономия токенов

- Перед работой читать этот файл вместо длинной истории.
- Агенты пишут lead только финал, блокер или риск.
- Manager сжимает отчеты агентов до 5-10 строк.
- Не вставлять HTML, длинные логи, полный diff и повторные промты.
- QA делает только короткий smoke-check по чеклисту.
